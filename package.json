{
  "name": "easy-click-semantic-keyboard-navigator",
  "version": "1.0.0",
  "description": "Chrome extension that enables mouse-free navigation using semantic, ergonomic keyboard hints.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true });\""
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "esbuild": "^0.25.0",
    "@types/chrome": "^0.0.300"
  }
}
